<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#00FA9A</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#0000FF</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#32CD32
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#32CD32
        </item>
    </style>
    
    <!--Switch Color -->
    <color name="vozativado">#32CD32</color>
    <color name="vozativado2">#32CD32</color>
    <color name="manualativado">#0000FF</color>


</resources>
